# .gitignore for Foundry (Forge) + optional Node tooling
# Add or remove entries according to what you want to commit.

# Foundry build / cache
out/
cache/

# Ignore development broadcast logs except keep the folder itself
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Docs
docs/

# Dotenv / secrets
.env
.env.local
*.env
*.secret
*.key
*.keystore

# Node / JS
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml
package-lock.json
yarn.lock

# Dependencies folder
# If you vendor dependencies (commit lib/), remove this line. If you use forge install every machine,
# you may ignore lib/ to avoid large vendor files in repo.
lib/

# Anvil / local node data
anvil.db
anvil-*
.anvil/

# IDEs and OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Coverage / artifacts / build outputs
coverage/
artifacts/
dist/
build/
cache-hh/
typechain/
tsconfig.tsbuildinfo

# Logs and temporary
*.log
*.tmp
*.bak

# Misc
*.swp

# End of .gitignore
